#include "main.h"
/**
 * main - main block
 * Return: 0 (Success)
 */
int main(void)
{
char outputString[] = "_putchar\n";
for (int i = 0; outputString[i] != '\0'; i++)
{
putchar(outputString[i]);
}
return (0);
}
